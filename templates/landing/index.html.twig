{% extends 'base.html.twig' %}

{% block title %}Bienvenido - Gabinete IT{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #e3f2fd, #ffffff);
        }
        .hero {
            padding: 6rem 1rem;
            text-align: center;
        }
        .hero h1 {
            font-size: 3rem;
            font-weight: bold;
        }
        .hero p {
            font-size: 1.2rem;
            color: #555;
        }
        .hero .btn {
            padding: 0.75rem 1.5rem;
            font-size: 1.1rem;
        }
        .feature-icon {
            font-size: 2rem;
            color: #0d6efd;
        }
    </style>
{% endblock %}

{% block body %}
<div class="hero">
    <h1>👨‍💻 Bienvenido al Sistema Gabinete IT</h1>
    <p class="mt-3 mb-4">Gestión eficiente de tareas técnicas, equipos y soporte informático.</p>
    <a href="{{ path('app_login') }}" class="btn btn-primary btn-lg">
        <i class="bi bi-box-arrow-in-right me-2"></i>Iniciar Sesión
    </a>
</div>

<div class="container text-center mt-5">
    <div class="row">
        <div class="col-md-4 mb-4">
            <i class="bi bi-clipboard-check feature-icon mb-2"></i>
            <h5>Tareas Técnicas</h5>
            <p>Asigná, editá y gestioná tareas con fecha límite y prioridad.</p>
        </div>
        <div class="col-md-4 mb-4">
            <i class="bi bi-cpu feature-icon mb-2"></i>
            <h5>Gestión de Equipos</h5>
            <p>Controlá equipos asignados, estado y ubicación.</p>
        </div>
        <div class="col-md-4 mb-4">
            <i class="bi bi-people feature-icon mb-2"></i>
            <h5>Trabajo en Equipo</h5>
            <p>Seguimiento claro de técnicos y tareas asignadas.</p>
        </div>
    </div>
</div>

{% endblock %}
